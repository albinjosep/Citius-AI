# Backend Environment Variables
MONGO_URL=mongodb://localhost:27017
DB_NAME=your_database_name
CORS_ORIGINS=http://localhost:3000,https://yourdomain.vercel.app

# Frontend Environment Variables (create .env.local in frontend directory)
REACT_APP_BACKEND_URL=http://localhost:8000

# Production Environment Variables for Vercel
# Set these in Vercel dashboard:
# MONGO_URL=your_production_mongodb_url
# DB_NAME=your_production_database_name
# CORS_ORIGINS=https://yourdomain.vercel.app
# REACT_APP_BACKEND_URL=https://yourdomain.vercel.app/api

# Note: The .npmrc files are configured to handle React 19 peer dependency conflicts
# If you encounter npm install errors during deployment, the --legacy-peer-deps flag is automatically applied